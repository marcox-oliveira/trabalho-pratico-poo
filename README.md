# trabalho-pratico-poo
Plataforma de streaming de música em Python aplicando POO (herança, polimorfismo, classes abstratas, encapsulamento), persistência em PostgreSQL via psycopg2 e organização em pacotes (models, repositories, services, ui, database).
